import Axios from 'axios'

// Create an axios instance to submit form data and query the CMS API
// For creating form component storybooks, use this as the axios instance to mock against
export const axios = Axios.create()
